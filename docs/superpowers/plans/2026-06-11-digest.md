# Digest au démarrage (rappel automatique) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Injecter au `SessionStart` un **digest borné** des descriptions de faits (groupées par domaine, `⚠` si périmé) pour rendre le rappel **automatique**, sans coût par message ni fastembed.

**Architecture :** Un nouveau module `scripts/digest.py` expose `build_digest(vault, max_lines=120, today=None) -> str`, qui réutilise `collect_facts` (de `build-viewer.py`) et la règle de péremption SP2 (`STALE_DAYS=90`). Le hook SP1 `hook-memory.sh start` appelle `python3 digest.py "$clone"` et émet le digest **avant** le rappel synchro/promote existant. Pur lecture de fichiers, best-effort, jamais bloquant.

**Tech Stack :** Python 3 (stdlib seule : `datetime`, `importlib`, `os`), bash, `unittest`.

**Référence design :** `docs/superpowers/specs/2026-06-11-digest-design.md`.

**Convention du programme :** chaque chantier met à jour la **doc ET les tests** (cf. mémoire `chantier-doc-tests-convention`).

---

## File Structure

| Fichier | Responsabilité | Action |
|---|---|---|
| `scripts/digest.py` | `build_digest(vault, max_lines, today)` + CLI ; réutilise `collect_facts` et la péremption. | Créer |
| `tests/test_digest.py` | tests unitaires de `build_digest` (groupé, périmé, dégradé, vide, patterns). | Créer |
| `scripts/hook-memory.sh` | en mode `start`, émettre le digest avant le message synchro/promote. | Modifier |
| `tests/test_hooks.py` | un test : `hook start` sur vault branché → sortie contient une description de fait. | Modifier |
| `docs/ARCHITECTURE.md` | §12 : le digest = rappel automatique borné au démarrage. | Modifier |

**Conventions réutilisées (déjà sur la branche) :**
- `scripts/build-viewer.py` : `collect_facts(vault, include_body=True) -> (facts, index_body)`. Chaque `fact` est un dict `{file, name, description, type, reviewed, domain, path}` (et `body` si `include_body`). `index_body` = corps de `MEMORY.md` (la carte).
- Péremption SP2 (`assets/viewer-template.html`) : `STALE_DAYS = 90`, périmé si `reviewed` absent OU `(aujourd'hui - reviewed).days >= 90`.
- Tests : import par `importlib.util.spec_from_file_location` (les scripts ont un tiret → pas importables par `import`).

---

## Task 1 : `scripts/digest.py` + tests

**Files:**
- Create: `scripts/digest.py`
- Create: `tests/test_digest.py`

- [ ] **Step 1 : Écrire les tests d'abord (`tests/test_digest.py`)**

```python
import datetime
import importlib.util
import os
import tempfile
import unittest

HERE = os.path.dirname(__file__)
SPEC = importlib.util.spec_from_file_location(
    "digest", os.path.join(HERE, "..", "scripts", "digest.py")
)
dg = importlib.util.module_from_spec(SPEC)
SPEC.loader.exec_module(dg)

TODAY = datetime.date(2026, 6, 11)
FRESH = "2026-06-01"          # 10 j -> frais
OLD = "2026-01-01"           # ~161 j -> périmé


def fact_md(name, desc, type_="project", reviewed=FRESH):
    return ("---\nname: %s\ndescription: %s\nmetadata:\n  type: %s\n  reviewed: %s\n---\ncorps\n"
            % (name, desc, type_, reviewed))


def write(path, content):
    os.makedirs(os.path.dirname(path), exist_ok=True)
    with open(path, "w", encoding="utf-8") as f:
        f.write(content)


class BuildDigestTest(unittest.TestCase):
    def setUp(self):
        self._tmp = tempfile.TemporaryDirectory()
        self.vault = self._tmp.name

    def tearDown(self):
        self._tmp.cleanup()

    def test_empty_vault_returns_empty(self):
        self.assertEqual(dg.build_digest(self.vault, today=TODAY), "")

    def test_lists_descriptions_grouped_by_domain(self):
        write(os.path.join(self.vault, "mailing", "relance.md"),
              fact_md("relance", "Relancer après 3 jours sans réponse"))
        write(os.path.join(self.vault, "facturation", "tva.md"),
              fact_md("tva", "TVA à 20% sur les prestations"))
        out = dg.build_digest(self.vault, today=TODAY)
        self.assertIn("Relancer après 3 jours sans réponse", out)
        self.assertIn("TVA à 20% sur les prestations", out)
        self.assertIn("mailing", out)
        self.assertIn("facturation", out)
        # En-tête avec le compte total.
        self.assertIn("2 faits", out)

    def test_stale_fact_marked_fresh_not(self):
        write(os.path.join(self.vault, "mailing", "vieux.md"),
              fact_md("vieux", "Fait pas revu depuis longtemps", reviewed=OLD))
        write(os.path.join(self.vault, "mailing", "neuf.md"),
              fact_md("neuf", "Fait revu hier", reviewed=FRESH))
        out = dg.build_digest(self.vault, today=TODAY)
        # La ligne du fait périmé porte un ⚠, celle du frais non.
        vieux_line = next(l for l in out.splitlines() if "Fait pas revu" in l)
        neuf_line = next(l for l in out.splitlines() if "Fait revu hier" in l)
        self.assertIn("⚠", vieux_line)
        self.assertNotIn("⚠", neuf_line)

    def test_missing_reviewed_is_stale(self):
        write(os.path.join(self.vault, "mailing", "sansdate.md"),
              "---\nname: sansdate\ndescription: Aucune date de revue\nmetadata:\n  type: project\n---\nx\n")
        out = dg.build_digest(self.vault, today=TODAY)
        line = next(l for l in out.splitlines() if "Aucune date de revue" in l)
        self.assertIn("⚠", line)

    def test_over_budget_degraded(self):
        for i in range(5):
            write(os.path.join(self.vault, "mailing", "f%d.md" % i),
                  fact_md("f%d" % i, "Description unique numero %d" % i))
        out = dg.build_digest(self.vault, max_lines=3, today=TODAY)
        # Dégradé : compte + domaines + renvoi recherche, PAS toutes les descriptions.
        self.assertIn("digest complet trop volumineux", out)
        self.assertIn("search_memory", out)
        self.assertIn("mailing", out)
        self.assertNotIn("Description unique numero 0", out)

    def test_includes_personal_facts(self):
        write(os.path.join(self.vault, "feedback_style.md"),
              fact_md("style", "Toujours répondre en français", type_="feedback"))
        out = dg.build_digest(self.vault, today=TODAY)
        self.assertIn("Toujours répondre en français", out)

    def test_includes_patterns_section_from_memory(self):
        write(os.path.join(self.vault, "mailing", "a.md"), fact_md("a", "un fait quelconque"))
        write(os.path.join(self.vault, "MEMORY.md"),
              "# Carte\n\n## Domaines\n- mailing\n\n## Patterns & Conventions\n- Toujours dater les faits\n")
        out = dg.build_digest(self.vault, today=TODAY)
        self.assertIn("Patterns & Conventions", out)
        self.assertIn("Toujours dater les faits", out)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2 : Lancer les tests pour vérifier qu'ils échouent**

Run : `python3 -m unittest tests.test_digest -v`
Expected : FAIL/ERROR — `scripts/digest.py` n'existe pas encore (`ModuleNotFoundError` / `exec_module` échoue).

- [ ] **Step 3 : Écrire `scripts/digest.py`**

```python
#!/usr/bin/env python3
"""Digest mémoire au démarrage : description compacte d'un vault, bornée.

`build_digest(vault, max_lines=120, today=None) -> str` réutilise `collect_facts`
(de build-viewer.py) et la règle de péremption SP2 (STALE_DAYS=90). Pur lecture de
fichiers, aucun fastembed. Émis par `hook-memory.sh start` au SessionStart.

- Sous le budget (len(facts) <= max_lines) : une ligne par fait, groupée par domaine,
  avec ⚠ si périmé ; inclut la section « Patterns & Conventions » de MEMORY.md.
- Au-dessus : digest dégradé (domaines + comptes + renvoi search_memory/-list).
- Vault vide : chaîne vide (rien à émettre).
"""
import datetime
import importlib.util
import os
import sys

_HERE = os.path.dirname(os.path.abspath(__file__))
_SPEC = importlib.util.spec_from_file_location(
    "build_viewer", os.path.join(_HERE, "build-viewer.py")
)
_bv = importlib.util.module_from_spec(_SPEC)
_SPEC.loader.exec_module(_bv)

STALE_DAYS = 90


def _is_stale(reviewed, today):
    """Périmé si `reviewed` absent/illisible, ou vieux d'au moins STALE_DAYS jours (SP2)."""
    if not reviewed:
        return True
    try:
        d = datetime.date.fromisoformat(reviewed)
    except ValueError:
        return True
    return (today - d).days >= STALE_DAYS


def _patterns_section(index_body):
    """Renvoie la section markdown dont le titre contient « Patterns » (sinon "")."""
    lines = index_body.splitlines()
    out, capturing = [], False
    for line in lines:
        is_heading = line.startswith("#")
        if is_heading and "pattern" in line.lower():
            capturing = True
            out.append(line)
            continue
        if capturing and is_heading:   # heading suivant -> fin de section
            break
        if capturing:
            out.append(line)
    return "\n".join(out).strip()


def build_digest(vault, max_lines=120, today=None):
    today = today or datetime.date.today()
    facts, index_body = _bv.collect_facts(vault, include_body=False)
    n = len(facts)
    if n == 0:
        return ""

    by_domain = {}
    for f in facts:
        by_domain.setdefault(f["domain"], []).append(f)
    domains = sorted(by_domain)

    if n > max_lines:
        lines = ["## Mémoire d'équipe (%d faits, %d domaines) — digest complet trop volumineux"
                 % (n, len(domains))]
        for d in domains:
            lines.append("- %s — %d fait(s)" % (d, len(by_domain[d])))
        lines.append("Pour le détail, utilise `search_memory` (MCP) ou `/memory-list`.")
        return "\n".join(lines)

    lines = ["## Mémoire d'équipe (%d faits)" % n]
    for d in domains:
        lines.append("")
        lines.append("### %s" % d)
        for f in by_domain[d]:
            warn = "⚠ " if _is_stale(f["reviewed"], today) else ""
            lines.append("- %s`%s` — %s · %s" % (warn, f["name"], f["description"], f["type"]))

    patterns = _patterns_section(index_body)
    if patterns:
        lines.append("")
        lines.append(patterns)

    return "\n".join(lines)


if __name__ == "__main__":
    vault = sys.argv[1] if len(sys.argv) > 1 else "."
    out = build_digest(vault)
    if out:
        print(out)
```

- [ ] **Step 4 : Lancer les tests pour vérifier qu'ils passent**

Run : `python3 -m unittest tests.test_digest -v`
Expected : PASS (7 tests).

- [ ] **Step 5 : Fumée CLI**

Run :
```bash
mkdir -p /tmp/dgvault/mailing && printf -- '---\nname: relance\ndescription: Relancer apres 3j\nmetadata:\n  type: project\n  reviewed: 2026-06-01\n---\nx\n' > /tmp/dgvault/mailing/relance.md
python3 scripts/digest.py /tmp/dgvault
```
Expected : affiche `## Mémoire d'équipe (1 faits)`, `### mailing`, et `- `relance` — Relancer apres 3j · project`.

- [ ] **Step 6 : Commit**

```bash
git add scripts/digest.py tests/test_digest.py
git commit -m "feat(digest): build_digest — rappel borné groupé par domaine (péremption SP2)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2 : Intégration au hook `start` + test

**Files:**
- Modify: `scripts/hook-memory.sh`
- Modify: `tests/test_hooks.py`

- [ ] **Step 1 : Ajouter le test du hook (`tests/test_hooks.py`)**

Dans `tests/test_hooks.py`, dans la classe `HookScriptTest` (après `test_noop_when_not_branched`), insérer cette méthode. Le `FACT` du module n'a pas de `description` exploitable ; on écrit un fait avec une description discriminante directement.

```python
    def test_start_emits_digest_with_fact_description(self):
        with tempfile.TemporaryDirectory() as d:
            clone = os.path.join(d, "clone")
            os.makedirs(clone)
            init_repo(clone)
            write(clone, "mailing/relance.md",
                  "---\nname: relance\ndescription: Relancer apres trois jours\n"
                  "metadata:\n  type: project\n  reviewed: 2026-06-01\n---\nx\n")
            git(clone, "add", "-A")
            git(clone, "commit", "-qm", "base")
            reg = os.path.join(d, "registry.json")
            with open(reg, "w") as f:
                json.dump({"projets": [{"slug": "-tmp-proj", "clone": clone}]}, f)
            rc, out = run_hook("start", "/tmp/proj", reg)
            self.assertEqual(rc, 0)
            self.assertIn("Relancer apres trois jours", out)
            self.assertIn("Mémoire d'équipe", out)
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run : `python3 -m unittest tests.test_hooks.HookScriptTest.test_start_emits_digest_with_fact_description -v`
Expected : FAIL — le hook n'émet pas encore le digest (`Relancer apres trois jours` absent).

- [ ] **Step 3 : Intégrer le digest dans `scripts/hook-memory.sh`**

Dans `scripts/hook-memory.sh`, juste **avant** la ligne finale `[ -n "$msg" ] && printf '%s\n' "$msg"` (ligne 31), insérer le bloc d'émission du digest (mode `start` uniquement, best-effort) :

```bash
if [ "$MODE" = "start" ]; then
  digest="$(python3 "$HERE/digest.py" "$clone" 2>/dev/null)"
  [ -n "$digest" ] && printf '%s\n' "$digest"
fi

[ -n "$msg" ] && printf '%s\n' "$msg"
exit 0
```

Le résultat : le `start` émet le **digest d'abord**, puis le message synchro/promote. Le mode `end` est inchangé (pas de digest). Best-effort : si `python3`/`digest.py` échoue, `digest` est vide et rien n'est émis.

- [ ] **Step 4 : Lancer le test pour vérifier qu'il passe**

Run : `python3 -m unittest tests.test_hooks.HookScriptTest.test_start_emits_digest_with_fact_description -v`
Expected : PASS.

- [ ] **Step 5 : Non-régression du hook (le `end` reste silencieux/inchangé)**

Run : `python3 -m unittest tests.test_hooks -v`
Expected : PASS (tous les tests hook, dont `test_end_silent_when_clean` et `test_noop_when_not_branched`).

- [ ] **Step 6 : Commit**

```bash
git add scripts/hook-memory.sh tests/test_hooks.py
git commit -m "feat(hook): émettre le digest au SessionStart avant le rappel synchro/promote

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3 : Documentation (`docs/ARCHITECTURE.md` §12)

**Files:**
- Modify: `docs/ARCHITECTURE.md`

- [ ] **Step 1 : Repérer la section §12 et sa sous-section hooks**

Run : `grep -n "^## 12\|hook\|SessionStart\|Boucle vivante" docs/ARCHITECTURE.md | head -20`
Expected : afficher le numéro de ligne de la section `## 12` et de la sous-section « Boucle vivante / hooks » à compléter.

- [ ] **Step 2 : Ajouter la sous-section digest à la fin de §12**

Dans `docs/ARCHITECTURE.md`, à la **fin de la section `## 12`** (juste avant la section `## 13` si elle existe, sinon en fin de §12), insérer :

```markdown
### Digest au démarrage (rappel automatique)

Nativement, seule la **carte** `MEMORY.md` (les domaines) est chargée au `SessionStart` —
**pas le contenu des faits**. Le **digest** comble ce trou : `scripts/digest.py`
(`build_digest(vault, max_lines=120)`) lit le vault (réutilise `collect_facts`) et émet, via
`hook-memory.sh start`, **une description d'une ligne par fait, groupée par domaine**, avant le
rappel synchro/promote. Claude **sait** alors ce qui existe et lit le fait quand le sujet arrive,
**sans qu'on le lui demande** — le rappel devient quasi mains-libres.

C'est **borné** (principe du sharding) : sous `max_lines` (120), digest complet ; un fait périmé
(`reviewed` ≥ 90 j ou absent, cf. fraîcheur SP2) porte un **`⚠`** ; la section
« Patterns & Conventions » de `MEMORY.md` est jointe. Au-dessus du budget → digest **dégradé**
(domaines + comptes + renvoi `search_memory`/`/memory-list`). **Pur lecture de fichiers, aucun
fastembed** : marche toujours, coût borné (~2 k tokens au pire), zéro coût par message.
```

- [ ] **Step 3 : Vérifier**

Run : `grep -c "Digest au démarrage\|build_digest\|digest.py" docs/ARCHITECTURE.md`
Expected : ≥ 3.

- [ ] **Step 4 : Commit**

```bash
git add docs/ARCHITECTURE.md
git commit -m "docs(digest): ARCHITECTURE §12 — le digest = rappel automatique borné

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4 : Vérification

- [ ] **Step 1 : Suite complète (non-régression)**

Run : `python3 -m unittest discover -s . -p 'test_*.py' 2>&1 | tail -3`
Expected : OK — tous les tests passent (les 117 existants + les 7 de `test_digest` + 1 hook = 125).

- [ ] **Step 2 : Fumée hook réelle (mode start, vault temporaire branché)**

Run :
```bash
TMP=$(mktemp -d); CLONE="$TMP/clone"; mkdir -p "$CLONE/mailing"
git -C "$CLONE" init -q && git -C "$CLONE" config user.email t@t && git -C "$CLONE" config user.name t
printf -- '---\nname: relance\ndescription: Relancer apres trois jours sans reponse\nmetadata:\n  type: project\n  reviewed: 2026-06-01\n---\nx\n' > "$CLONE/mailing/relance.md"
git -C "$CLONE" add -A && git -C "$CLONE" commit -qm base
printf '{"projets":[{"slug":"-tmp-proj","clone":"%s"}]}' "$CLONE" > "$TMP/reg.json"
CLAUDE_PROJECT_DIR=/tmp/proj SM_REGISTRY="$TMP/reg.json" bash scripts/hook-memory.sh start
rm -rf "$TMP"
```
Expected : la sortie contient `## Mémoire d'équipe (1 faits)`, `### mailing`, et `Relancer apres trois jours sans reponse`.

- [ ] **Step 3 : Relecture**

Vérifier de visu : `digest.py` n'utilise aucune dépendance externe (stdlib seule) ; le hook reste best-effort (`2>/dev/null`, jamais bloquant, `exit 0`) ; le digest n'est émis qu'en mode `start`. La doc §12 décrit le bornage et le `⚠`.

---

## Self-Review

**Couverture de la spec :**

| Élément du design | Tâche |
|---|---|
| `scripts/digest.py` : `build_digest(vault, max_lines=120)` | Task 1 (Step 3) |
| Réutilise `collect_facts` + péremption `reviewed` (SP2) | Task 1 (`_bv.collect_facts`, `_is_stale` STALE_DAYS=90) |
| En-tête `## Mémoire d'équipe (N faits)` | Task 1 (Step 3) + test `test_lists_descriptions_grouped_by_domain` |
| Groupé par domaine, ligne `nom — description · type` | Task 1 (Step 3) + test |
| `⚠` si périmé (≥90 j ou absent) | Task 1 + tests `test_stale_fact_marked_fresh_not`, `test_missing_reviewed_is_stale` |
| Inclut faits perso (`user`/`feedback`) | Task 1 (collect_facts inclut tout) + test `test_includes_personal_facts` |
| Section « Patterns & Conventions » de MEMORY.md | Task 1 (`_patterns_section`) + test `test_includes_patterns_section_from_memory` |
| Digest dégradé au-dessus du budget | Task 1 + test `test_over_budget_degraded` |
| Vault vide → rien | Task 1 + test `test_empty_vault_returns_empty` |
| Intégration `hook-memory.sh start`, digest avant le msg | Task 2 (Step 3) + test `test_start_emits_digest_with_fact_description` |
| Mode `end` inchangé, best-effort, exit 0 | Task 2 (Step 3, Step 5 non-régression) |
| Doc ARCHITECTURE §12 | Task 3 |
| Vérification (suite + fumée hook réelle) | Task 4 |

**Placeholders :** aucun — `digest.py`, `test_digest.py`, l'edit hook et l'edit doc sont fournis intégralement.

**Cohérence des types :** `build_digest(vault, max_lines=120, today=None)` — même signature dans le module, les tests et la CLI. `collect_facts(vault, include_body=False)` renvoie bien `(facts, index_body)` (vérifié dans `build-viewer.py`). Les clés de fait utilisées (`name`, `description`, `type`, `reviewed`, `domain`) existent toutes dans le dict produit par `collect_facts`. `_is_stale(reviewed, today)` et `STALE_DAYS=90` calquent la règle JS SP2 (`days >= STALE_DAYS`). Le paramètre `today` rend la péremption testable de façon déterministe (évite `datetime.date.today()` non figeable).

**Note tests :** `test_digest` fige `today=date(2026,6,11)` ; `FRESH=2026-06-01` (10 j → frais), `OLD=2026-01-01` (>90 j → périmé). Le test hook n'a pas de date figée mais utilise `reviewed=2026-06-01` : il vérifie la **présence de la description**, pas le `⚠` (robuste au temps qui passe).
