# CRUD local des faits dans le viewer — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permettre de créer / éditer / supprimer / déplacer des faits et renommer un domaine **directement dans le viewer**, en écrivant **uniquement dans la working copy locale** (étage 1) — la gouvernance (`/memory-promote` → `/memory-review`) reste intacte.

**Architecture :** On étend `serve-viewer.py` avec des routes `/api/*` (POST/PUT/DELETE). Les opérations sont des **fonctions module pures-ish** (mutation de fichier dans le clone + `reshard(vault)` pour régénérer les index) qui lèvent `ApiError(status, message)` ; les handlers HTTP parsent, vérifient un **jeton same-origin**, appellent ces fonctions et renvoient les **métadonnées fraîches**. Le viewer (`viewer-template.html`) gagne un vrai formulaire de création (remplace le snippet), des actions éditer/supprimer sur la carte, un crayon de renommage de domaine, et un bandeau « local ».

**Tech Stack :** Python 3 stdlib (`http.server`, `json`, `re`, `secrets`). Réutilise `build-viewer.py` (`collect_facts`/`parse_md`) et `reshard.py`. Tests : `unittest` (pattern `http.server` en thread + `urllib`, déjà en place dans `tests/test_serve_viewer.py`). UI : JS vanilla, vérif manuelle dans le viewer live.

**Référence design :** `docs/superpowers/specs/2026-06-10-ui-crud-design.md`.

**Garde-fou :** aucune écriture vers le canonique, aucun git dans l'UI. Le serveur écrit dans le **clone** (chemin reçu en argv), jamais ailleurs (garde anti-traversal `realpath` + `.md` only, déjà existante pour `/fact`).

---

## File Structure

| Fichier | Responsabilité | Action |
|---|---|---|
| `scripts/serve-viewer.py` | Sert le viewer (GET) **+ écritures `/api/*`** : helpers de validation, fonctions `create_fact`/`update_fact`/`delete_fact`/`rename_domain`, jeton CSRF, handlers `do_POST`/`do_PUT`/`do_DELETE`. | Modifier |
| `assets/viewer-template.html` | Formulaire de création (remplace le snippet), édition/suppression sur la carte, renommage de domaine, bandeau « local », helper `api()` avec jeton. | Modifier |
| `tests/test_serve_viewer.py` | Tests HTTP des écritures (création/édition/suppression/déplacement/renommage, validations, jeton). | Modifier |

---

# Phase 1 — Cœur CRUD (créer / éditer / supprimer)

## Task 1 : Serveur — plumbing + `create_fact` + `POST /api/fact` + jeton

**Files:**
- Modify: `scripts/serve-viewer.py`
- Test: `tests/test_serve_viewer.py`

- [ ] **Step 1 : Écrire les tests qui échouent**

Ajouter dans `tests/test_serve_viewer.py` (en haut, après les imports existants, un helper de requête écrivante ; puis une classe de tests). D'abord les imports/helper :

```python
import urllib.request


def _token_of(base_get):
    """Extrait le jeton CSRF injecté dans la page (DATA.token)."""
    html = base_get
    data = json.loads(html[html.index("<x>") + 3: html.index("</x>")])
    return data.get("token")


def write_req(port, method, path, body=None, token="SKIP"):
    url = "http://127.0.0.1:%d%s" % (port, path)
    data = json.dumps(body).encode("utf-8") if body is not None else None
    req = urllib.request.Request(url, data=data, method=method)
    if data is not None:
        req.add_header("Content-Type", "application/json")
    if token != "SKIP":
        req.add_header("X-SM-Token", token)
    return urllib.request.urlopen(req)
```

Puis la classe (avant `if __name__`) :

```python
class CreateTest(ServerTestBase):
    def _token(self):
        _, html = self.get("/")
        return _token_of(html)

    def test_get_injects_token(self):
        self.assertTrue(self._token())

    def test_create_without_token_is_403(self):
        with self.assertRaises(urllib.error.HTTPError) as cm:
            write_req(self.port, "POST", "/api/fact",
                      {"name": "x", "type": "project", "description": "d", "body": "b", "domain": "mailing"})
        self.assertEqual(cm.exception.code, 403)

    def test_create_writes_fact_and_index(self):
        r = write_req(self.port, "POST", "/api/fact",
                      {"name": "relance", "type": "project", "description": "relance 72h",
                       "body": "corps", "domain": "mailing"}, token=self._token())
        self.assertEqual(r.status, 200)
        self.assertTrue(os.path.isfile(os.path.join(self.vault, "mailing", "relance.md")))
        self.assertIn("relance", open(os.path.join(self.vault, "index", "mailing.md"), encoding="utf-8").read())

    def test_create_personal_goes_to_root_not_indexed(self):
        write_req(self.port, "POST", "/api/fact",
                  {"name": "note-perso", "type": "feedback", "description": "d", "body": "b", "domain": "mailing"},
                  token=self._token())
        self.assertTrue(os.path.isfile(os.path.join(self.vault, "note-perso.md")))   # racine, pas mailing/
        self.assertFalse(os.path.isfile(os.path.join(self.vault, "mailing", "note-perso.md")))

    def test_create_invalid_slug_is_400(self):
        with self.assertRaises(urllib.error.HTTPError) as cm:
            write_req(self.port, "POST", "/api/fact",
                      {"name": "Pas Valide", "type": "project", "description": "d", "body": "b", "domain": "mailing"},
                      token=self._token())
        self.assertEqual(cm.exception.code, 400)

    def test_create_duplicate_is_400(self):
        body = {"name": "audit", "type": "project", "description": "d", "body": "b", "domain": "mailing"}
        with self.assertRaises(urllib.error.HTTPError) as cm:    # 'audit' existe déjà dans le vault de test
            write_req(self.port, "POST", "/api/fact", body, token=self._token())
        self.assertEqual(cm.exception.code, 400)
```

(`ServerTestBase` crée déjà `mailing/audit.md` ; `json`, `os`, `urllib.error` sont déjà importés dans ce fichier.)

- [ ] **Step 2 : Lancer, vérifier l'échec**

Run : `python3 -m unittest tests.test_serve_viewer.CreateTest -v`
Expected : FAIL — pas de jeton injecté / pas de route `/api/fact` (403 ou 404 inattendus, `KeyError` token).

- [ ] **Step 3 : Implémenter dans `scripts/serve-viewer.py`**

(a) En tête, ajouter les imports et charger `reshard`. Remplacer le bloc de chargement de `build_viewer` :

```python
_HERE = os.path.dirname(os.path.abspath(__file__))
_SPEC = importlib.util.spec_from_file_location(
    "build_viewer", os.path.join(_HERE, "build-viewer.py"))
bv = importlib.util.module_from_spec(_SPEC)
_SPEC.loader.exec_module(bv)
```

par :

```python
_HERE = os.path.dirname(os.path.abspath(__file__))


def _load(name, filename):
    spec = importlib.util.spec_from_file_location(name, os.path.join(_HERE, filename))
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod


bv = _load("build_viewer", "build-viewer.py")
reshard = _load("reshard", "reshard.py")

SLUG_RE = re.compile(r"^[a-z0-9-]+$")
TYPES = {"project", "reference", "user", "feedback"}


class ApiError(Exception):
    def __init__(self, status, message):
        super().__init__(message)
        self.status = status
        self.message = message
```

et ajouter `import re` et `import secrets` aux imports du fichier (avec les autres).

(b) Ajouter les fonctions d'écriture (niveau module, après la classe `ApiError`) :

```python
def _fact_text(name, description, type_, body):
    return ("---\nname: %s\ndescription: %s\nmetadata:\n  type: %s\n---\n%s\n"
            % (name, description, type_, body))


def _validate(data):
    name = (data.get("name") or "").strip()
    typ = (data.get("type") or "").strip()
    if not SLUG_RE.match(name):
        raise ApiError(400, "nom invalide (slug attendu : a-z 0-9 -)")
    if typ not in TYPES:
        raise ApiError(400, "type invalide")
    domain = (data.get("domain") or "").strip()
    if typ in ("user", "feedback"):
        domain = ""                              # perso -> racine, jamais en domaine
    elif domain and not SLUG_RE.match(domain):
        raise ApiError(400, "domaine invalide (slug attendu)")
    return name, typ, domain, data.get("description", "") or "", data.get("body", "") or ""


def _rel_for(name, domain):
    return (domain + "/" + name + ".md") if domain else (name + ".md")


def _safe_path(vault, rel):
    vault_real = os.path.realpath(vault)
    try:
        full = os.path.realpath(os.path.join(vault, rel))
    except (ValueError, OSError):
        raise ApiError(404, "chemin invalide")
    inside = full == vault_real or full.startswith(vault_real + os.sep)
    if not rel or not inside or not full.endswith(".md"):
        raise ApiError(404, "chemin hors vault")
    return full


def _metadata(vault):
    facts, index_body = bv.collect_facts(vault, include_body=False)
    return {"facts": facts, "index": index_body, "vault": vault, "count": len(facts)}


def create_fact(vault, data):
    name, typ, domain, desc, body = _validate(data)
    full = _safe_path(vault, _rel_for(name, domain))
    if os.path.exists(full):
        raise ApiError(400, "un fait « %s » existe déjà ici" % name)
    os.makedirs(os.path.dirname(full), exist_ok=True)
    with open(full, "w", encoding="utf-8") as f:
        f.write(_fact_text(name, desc, typ, body))
    reshard.reshard(vault)
    return _metadata(vault)
```

(c) Dans `make_handler(vault, template)`, générer le jeton et l'injecter ; ajouter les méthodes d'écriture. Juste après `vault_real = os.path.realpath(vault)` ajouter :

```python
    token = secrets.token_hex(16)
```

Dans `do_GET`, brancher le jeton dans les données servies — remplacer :

```python
                data = {"facts": facts, "index": index_body, "vault": vault, "count": len(facts)}
```

par :

```python
                data = {"facts": facts, "index": index_body, "vault": vault,
                        "count": len(facts), "token": token}
```

Et ajouter, dans la classe `Handler` (après `do_GET`), les helpers + `do_POST` :

```python
        def _json_body(self):
            n = int(self.headers.get("Content-Length") or 0)
            raw = self.rfile.read(n) if n else b"{}"
            return json.loads(raw or b"{}")

        def _require_token(self):
            if self.headers.get("X-SM-Token") != token:
                raise ApiError(403, "jeton manquant ou invalide")

        def _ok(self, data):
            self._send(200, json.dumps(data, ensure_ascii=False),
                       "application/json; charset=utf-8")

        def do_POST(self):
            u = urlparse(self.path)
            try:
                self._require_token()
                if u.path == "/api/fact":
                    self._ok(create_fact(vault, self._json_body()))
                else:
                    self._send(404, "not found")
            except ApiError as e:
                self._send(e.status, e.message)
            except (ValueError, OSError) as e:
                self._send(400, "erreur: %s" % e)
```

- [ ] **Step 4 : Lancer, vérifier le succès**

Run : `python3 -m unittest tests.test_serve_viewer -v`
Expected : PASS (les tests existants + `CreateTest`, 6 nouveaux).

- [ ] **Step 5 : Commit**

```bash
git add scripts/serve-viewer.py tests/test_serve_viewer.py
git commit -m "feat(viewer): écritures /api/fact — création + jeton CSRF + validations (étage 1)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2 : Serveur — `update_fact` + `PUT /api/fact` (éditer + déplacer)

**Files:**
- Modify: `scripts/serve-viewer.py`
- Test: `tests/test_serve_viewer.py`

- [ ] **Step 1 : Ajouter les tests qui échouent**

Ajouter dans `tests/test_serve_viewer.py`, avant `if __name__` :

```python
class UpdateTest(ServerTestBase):
    def _token(self):
        _, html = self.get("/"); return _token_of(html)

    def test_update_changes_fields(self):
        write_req(self.port, "PUT", "/api/fact?f=mailing/audit.md",
                  {"name": "audit", "type": "reference", "description": "maj", "body": "neuf", "domain": "mailing"},
                  token=self._token())
        txt = open(os.path.join(self.vault, "mailing", "audit.md"), encoding="utf-8").read()
        self.assertIn("type: reference", txt)
        self.assertIn("neuf", txt)

    def test_update_rename_moves_file(self):
        write_req(self.port, "PUT", "/api/fact?f=mailing/audit.md",
                  {"name": "audit-2", "type": "project", "description": "d", "body": "b", "domain": "mailing"},
                  token=self._token())
        self.assertFalse(os.path.isfile(os.path.join(self.vault, "mailing", "audit.md")))
        self.assertTrue(os.path.isfile(os.path.join(self.vault, "mailing", "audit-2.md")))

    def test_update_change_domain_relocates(self):
        write_req(self.port, "PUT", "/api/fact?f=mailing/audit.md",
                  {"name": "audit", "type": "project", "description": "d", "body": "b", "domain": "ui"},
                  token=self._token())
        self.assertFalse(os.path.isfile(os.path.join(self.vault, "mailing", "audit.md")))
        self.assertTrue(os.path.isfile(os.path.join(self.vault, "ui", "audit.md")))

    def test_update_missing_is_404(self):
        with self.assertRaises(urllib.error.HTTPError) as cm:
            write_req(self.port, "PUT", "/api/fact?f=mailing/nope.md",
                      {"name": "nope", "type": "project", "description": "d", "body": "b", "domain": "mailing"},
                      token=self._token())
        self.assertEqual(cm.exception.code, 404)
```

- [ ] **Step 2 : Lancer, vérifier l'échec**

Run : `python3 -m unittest tests.test_serve_viewer.UpdateTest -v`
Expected : FAIL — pas de `do_PUT` (405/501) ni `update_fact`.

- [ ] **Step 3 : Implémenter**

(a) Ajouter la fonction (niveau module, après `create_fact`) :

```python
def update_fact(vault, file, data):
    old = _safe_path(vault, file)
    if not os.path.isfile(old):
        raise ApiError(404, "fait introuvable")
    name, typ, domain, desc, body = _validate(data)
    new = _safe_path(vault, _rel_for(name, domain))
    if new != old and os.path.exists(new):
        raise ApiError(400, "un fait « %s » existe déjà à cet emplacement" % name)
    os.makedirs(os.path.dirname(new), exist_ok=True)
    with open(new, "w", encoding="utf-8") as f:
        f.write(_fact_text(name, desc, typ, body))
    if new != old:
        os.remove(old)                       # renommé ou déplacé
    reshard.reshard(vault)
    return _metadata(vault)
```

(b) Ajouter `do_PUT` dans la classe `Handler` (après `do_POST`) :

```python
        def do_PUT(self):
            u = urlparse(self.path)
            try:
                self._require_token()
                if u.path == "/api/fact":
                    f = (parse_qs(u.query).get("f") or [""])[0]
                    self._ok(update_fact(vault, f, self._json_body()))
                else:
                    self._send(404, "not found")
            except ApiError as e:
                self._send(e.status, e.message)
            except (ValueError, OSError) as e:
                self._send(400, "erreur: %s" % e)
```

- [ ] **Step 4 : Lancer, vérifier le succès**

Run : `python3 -m unittest tests.test_serve_viewer -v`
Expected : PASS (+ `UpdateTest`, 4 nouveaux).

- [ ] **Step 5 : Commit**

```bash
git add scripts/serve-viewer.py tests/test_serve_viewer.py
git commit -m "feat(viewer): PUT /api/fact — éditer + renommer + déplacer entre domaines

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3 : Serveur — `delete_fact` + `DELETE /api/fact`

**Files:**
- Modify: `scripts/serve-viewer.py`
- Test: `tests/test_serve_viewer.py`

- [ ] **Step 1 : Ajouter les tests qui échouent**

Ajouter dans `tests/test_serve_viewer.py`, avant `if __name__` :

```python
class DeleteTest(ServerTestBase):
    def _token(self):
        _, html = self.get("/"); return _token_of(html)

    def test_delete_removes_fact(self):
        write_req(self.port, "DELETE", "/api/fact?f=mailing/audit.md", token=self._token())
        self.assertFalse(os.path.isfile(os.path.join(self.vault, "mailing", "audit.md")))

    def test_delete_missing_is_404(self):
        with self.assertRaises(urllib.error.HTTPError) as cm:
            write_req(self.port, "DELETE", "/api/fact?f=mailing/nope.md", token=self._token())
        self.assertEqual(cm.exception.code, 404)

    def test_delete_traversal_is_404(self):
        with self.assertRaises(urllib.error.HTTPError) as cm:
            write_req(self.port, "DELETE", "/api/fact?f=../../../etc/passwd", token=self._token())
        self.assertEqual(cm.exception.code, 404)
```

- [ ] **Step 2 : Lancer, vérifier l'échec**

Run : `python3 -m unittest tests.test_serve_viewer.DeleteTest -v`
Expected : FAIL — pas de `do_DELETE`.

- [ ] **Step 3 : Implémenter**

(a) Ajouter la fonction (après `update_fact`) :

```python
def delete_fact(vault, file):
    full = _safe_path(vault, file)
    if not os.path.isfile(full):
        raise ApiError(404, "fait introuvable")
    os.remove(full)
    reshard.reshard(vault)
    return _metadata(vault)
```

(b) Ajouter `do_DELETE` dans la classe `Handler` (après `do_PUT`) :

```python
        def do_DELETE(self):
            u = urlparse(self.path)
            try:
                self._require_token()
                if u.path == "/api/fact":
                    f = (parse_qs(u.query).get("f") or [""])[0]
                    self._ok(delete_fact(vault, f))
                else:
                    self._send(404, "not found")
            except ApiError as e:
                self._send(e.status, e.message)
            except (ValueError, OSError) as e:
                self._send(400, "erreur: %s" % e)
```

- [ ] **Step 4 : Lancer, vérifier le succès**

Run : `python3 -m unittest tests.test_serve_viewer -v`
Expected : PASS (+ `DeleteTest`, 3 nouveaux).

- [ ] **Step 5 : Commit**

```bash
git add scripts/serve-viewer.py tests/test_serve_viewer.py
git commit -m "feat(viewer): DELETE /api/fact — suppression (récupérable via git)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4 : UI — formulaire de création, édition/suppression, bandeau local

**Files:**
- Modify: `assets/viewer-template.html`

UI JS, vérifiée **manuellement** dans le viewer live (pas de harnais de test JS).

- [ ] **Step 1 : Ajouter le helper `api()` + le jeton + un bandeau « local »**

Dans le `<script>`, après la ligne `const $ = id => document.getElementById(id);`, ajouter :

```javascript
const TOKEN = DATA.token || '';
async function api(method, path, body){
  const r = await fetch(path, { method,
    headers: Object.assign({'X-SM-Token': TOKEN}, body ? {'Content-Type':'application/json'} : {}),
    body: body ? JSON.stringify(body) : undefined });
  if(!r.ok) throw new Error((await r.text()) || r.status);
  return r.json();
}
function applyMeta(meta){ DATA.facts = meta.facts; DATA.index = meta.index; DATA.count = meta.count; rebuildNav(); }
```

Dans la topbar HTML (après le `<div class="actions">…</div>` dans le `<header>`), ajouter un bandeau discret rappelant l'étage 1 — insérer juste avant `</header>` :

```html
      <span class="count" style="color:var(--faint)" title="Les écritures restent locales jusqu'à /memory-promote">⬤ local</span>
```

- [ ] **Step 2 : Remplacer la tab « Rédiger un fait » (snippet) par un vrai formulaire de création**

Remplacer la fonction `viewDraft()` par :

```javascript
function domainOptions(sel){
  const doms = [...new Set(DATA.facts.map(f => (f.path||[])[0]).filter(Boolean))].sort();
  return doms.map(d => `<option value="${esc(d)}"${d===sel?' selected':''}>${esc(d)}</option>`).join('');
}
function viewDraft(){
  return `<h3 class="view-h">Créer un fait</h3>
    <p class="view-p">Écrit en <b>local</b> (brouillon). Pour le partager : <code style="color:var(--coral)">/memory-promote</code>.</p>
    <div class="form">
      <label>nom (slug)</label><input id="d-name" placeholder="ex: regle-tva-export" autocomplete="off">
      <label>description</label><input id="d-desc" placeholder="une ligne discriminante" autocomplete="off">
      <label>type</label><select id="d-type"><option value="project">project</option><option value="reference">reference</option><option value="user">user (perso)</option><option value="feedback">feedback (perso)</option></select>
      <label>domaine</label><input id="d-domain" list="d-domains" placeholder="ex: mailing (vide = général)"><datalist id="d-domains">${domainOptions('')}</datalist>
      <label>contenu</label><textarea id="d-body" placeholder="Le fait, concis et autonome…"></textarea>
    </div>
    <div style="margin-top:10px"><button class="btn" id="d-create">créer le fait</button><span class="copied" id="d-msg" style="display:none"></span></div>`;
}
function bindDraft(){
  const v = id => ($(id) ? $(id).value.trim() : '');
  $('d-create').addEventListener('click', async () => {
    try {
      const meta = await api('POST', '/api/fact', { name:v('d-name'), description:v('d-desc'),
        type:$('d-type').value, body:$('d-body').value, domain:v('d-domain') });
      applyMeta(meta);
      state.view='fact'; state.factId = (v('d-domain') && $('d-type').value!=='user' && $('d-type').value!=='feedback')
        ? v('d-domain')+'/'+v('d-name')+'.md' : v('d-name')+'.md';
      update();
    } catch(e){ const m=$('d-msg'); m.style.display='inline'; m.style.color='var(--feedback)'; m.textContent = String(e.message||e); }
  });
}
```

- [ ] **Step 3 : Ajouter Éditer / Supprimer sur la carte de détail**

Remplacer la fonction `showFact(f)` par :

```javascript
function showFact(f){
  $('main').innerHTML = `<div class="card detail">
    <div class="fhead">${badge(f.type)}<span class="fname">${esc(f.name)}</span>
      <span style="margin-left:auto;display:flex;gap:8px">
        <button class="btn" id="f-edit">éditer</button>
        <button class="btn" id="f-del">supprimer</button></span></div>
    ${f.description ? `<div class="fdesc">${esc(f.description)}</div>` : ''}
    <div class="fbody" id="fbody">chargement…</div>
    <div class="ffile">${esc(f.file)}</div>
    <div id="f-confirm"></div>
  </div>`;
  fetch('/fact?f=' + encodeURIComponent(f.file))
    .then(r => { if(!r.ok) throw new Error(r.status); return r.text(); })
    .then(t => { const el = $('fbody'); if(el){ el.dataset.raw = t; el.innerHTML = md(t); } })
    .catch(() => { const el = $('fbody'); if(el) el.textContent = 'Erreur de chargement du fait.'; });
  $('f-edit').onclick = () => editFact(f);
  $('f-del').onclick = () => {
    $('f-confirm').innerHTML = `<div class="ud-note">Supprimer « ${esc(f.name)} » ? (récupérable via git tant que non poussé)
      <button class="btn" id="f-del-yes" style="margin-left:10px">confirmer</button>
      <button class="btn" id="f-del-no">annuler</button></div>`;
    $('f-del-no').onclick = () => $('f-confirm').innerHTML = '';
    $('f-del-yes').onclick = async () => {
      try { const meta = await api('DELETE', '/api/fact?f=' + encodeURIComponent(f.file)); applyMeta(meta);
        state.view='home'; state.factId=null; update(); }
      catch(e){ $('f-confirm').innerHTML = `<div class="ud-note" style="border-color:var(--feedback)">${esc(String(e.message||e))}</div>`; }
    };
  };
}
function editFact(f){
  const raw = ($('fbody') && $('fbody').dataset.raw) || '';
  const body = raw.replace(/^---[\s\S]*?---\n?/, '');   // corps sans frontmatter
  $('main').innerHTML = `<div class="card detail"><h3 class="view-h">Éditer un fait</h3>
    <div class="form">
      <label>nom (slug)</label><input id="e-name" value="${esc(f.name)}">
      <label>description</label><input id="e-desc" value="${esc(f.description||'')}">
      <label>type</label><select id="e-type">${['project','reference','user','feedback'].map(t=>`<option value="${t}"${t===f.type?' selected':''}>${t}</option>`).join('')}</select>
      <label>domaine</label><input id="e-domain" list="e-domains" value="${esc((f.path||[])[0]||'')}"><datalist id="e-domains">${domainOptions((f.path||[])[0]||'')}</datalist>
      <label>contenu</label><textarea id="e-body">${esc(body.trim())}</textarea>
    </div>
    <div style="margin-top:10px"><button class="btn" id="e-save">enregistrer</button>
      <button class="btn" id="e-cancel">annuler</button><span class="copied" id="e-msg" style="display:none"></span></div></div>`;
  $('e-cancel').onclick = () => { state.view='fact'; update(); };
  $('e-save').onclick = async () => {
    try {
      const meta = await api('PUT', '/api/fact?f=' + encodeURIComponent(f.file),
        { name:$('e-name').value.trim(), description:$('e-desc').value.trim(),
          type:$('e-type').value, body:$('e-body').value, domain:$('e-domain').value.trim() });
      applyMeta(meta);
      const dom = $('e-domain').value.trim(), ty = $('e-type').value, nm = $('e-name').value.trim();
      state.factId = (dom && ty!=='user' && ty!=='feedback') ? dom+'/'+nm+'.md' : nm+'.md';
      state.view='fact'; update();
    } catch(e){ const m=$('e-msg'); m.style.display='inline'; m.style.color='var(--feedback)'; m.textContent = String(e.message||e); }
  };
}
```

- [ ] **Step 4 : Régénérer le viewer servi et vérifier manuellement**

Run :
```bash
python3 scripts/serve-viewer.py /tmp/sm-crud-test assets/viewer-template.html 8898 &
mkdir -p /tmp/sm-crud-test/mailing && printf '%s' '---\nname: a\ndescription: d\nmetadata:\n  type: project\n---\nx' > /tmp/sm-crud-test/mailing/a.md
echo "Ouvrir http://127.0.0.1:8898"
```
Vérifier manuellement : créer un fait (apparaît dans l'arbre), l'éditer (champs persistés), changer son domaine (déplacé), le supprimer (confirmation → disparaît), créer un fait `feedback` (atterrit en « général »/racine). Arrêter : `kill %1`.

- [ ] **Step 5 : Commit**

```bash
git add assets/viewer-template.html
git commit -m "feat(viewer): UI CRUD — formulaire de création, édition/suppression, bandeau local

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

# Phase 2 — Organiser (renommer un domaine)

## Task 5 : Serveur — `rename_domain` + `POST /api/rename-domain` + patch `MEMORY.md`

**Files:**
- Modify: `scripts/serve-viewer.py`
- Test: `tests/test_serve_viewer.py`

- [ ] **Step 1 : Ajouter les tests qui échouent**

Ajouter dans `tests/test_serve_viewer.py`, avant `if __name__` :

```python
class RenameDomainTest(ServerTestBase):
    def _token(self):
        _, html = self.get("/"); return _token_of(html)

    def test_rename_moves_folder(self):
        write_req(self.port, "POST", "/api/rename-domain", {"old": "mailing", "new": "emailing"},
                  token=self._token())
        self.assertFalse(os.path.isdir(os.path.join(self.vault, "mailing")))
        self.assertTrue(os.path.isfile(os.path.join(self.vault, "emailing", "audit.md")))

    def test_rename_patches_memory(self):
        with open(os.path.join(self.vault, "MEMORY.md"), "w", encoding="utf-8") as f:
            f.write("# Carte\n\n## Domaines\n- **mailing** (1) → `index/mailing.md` — emails\n")
        write_req(self.port, "POST", "/api/rename-domain", {"old": "mailing", "new": "emailing"},
                  token=self._token())
        mem = open(os.path.join(self.vault, "MEMORY.md"), encoding="utf-8").read()
        self.assertIn("index/emailing.md", mem)
        self.assertNotIn("index/mailing.md", mem)

    def test_rename_to_existing_is_400(self):
        os.makedirs(os.path.join(self.vault, "ui"), exist_ok=True)
        with self.assertRaises(urllib.error.HTTPError) as cm:
            write_req(self.port, "POST", "/api/rename-domain", {"old": "mailing", "new": "ui"},
                      token=self._token())
        self.assertEqual(cm.exception.code, 400)

    def test_rename_missing_is_404(self):
        with self.assertRaises(urllib.error.HTTPError) as cm:
            write_req(self.port, "POST", "/api/rename-domain", {"old": "nope", "new": "x"},
                      token=self._token())
        self.assertEqual(cm.exception.code, 404)
```

- [ ] **Step 2 : Lancer, vérifier l'échec**

Run : `python3 -m unittest tests.test_serve_viewer.RenameDomainTest -v`
Expected : FAIL — pas de route `/api/rename-domain`.

- [ ] **Step 3 : Implémenter**

(a) Ajouter les fonctions (après `delete_fact`) :

```python
def _safe_dir(vault, name):
    vault_real = os.path.realpath(vault)
    full = os.path.realpath(os.path.join(vault, name))
    inside = full.startswith(vault_real + os.sep)
    if not name or not inside:
        raise ApiError(404, "domaine hors vault")
    return full


def _patch_memory_domain(vault, old, new):
    path = os.path.join(vault, "MEMORY.md")
    if not os.path.isfile(path):
        return
    txt = open(path, encoding="utf-8").read()
    txt = txt.replace("index/%s.md" % old, "index/%s.md" % new)   # le pointeur
    txt = txt.replace("**%s**" % old, "**%s**" % new)             # le libellé en gras s'il existe
    with open(path, "w", encoding="utf-8") as f:
        f.write(txt)


def rename_domain(vault, old, new):
    if not SLUG_RE.match(old or "") or not SLUG_RE.match(new or ""):
        raise ApiError(400, "domaine invalide (slug attendu)")
    old_dir, new_dir = _safe_dir(vault, old), _safe_dir(vault, new)
    if not os.path.isdir(old_dir):
        raise ApiError(404, "domaine introuvable")
    if os.path.exists(new_dir):
        raise ApiError(400, "le domaine « %s » existe déjà" % new)
    os.rename(old_dir, new_dir)
    _patch_memory_domain(vault, old, new)
    reshard.reshard(vault)
    return _metadata(vault)
```

(b) Dans `do_POST`, ajouter la branche avant le `else` :

```python
                elif u.path == "/api/rename-domain":
                    d = self._json_body()
                    self._ok(rename_domain(vault, d.get("old"), d.get("new")))
```

- [ ] **Step 4 : Lancer, vérifier le succès**

Run : `python3 -m unittest tests.test_serve_viewer -v`
Expected : PASS (+ `RenameDomainTest`, 4 nouveaux).

- [ ] **Step 5 : Commit**

```bash
git add scripts/serve-viewer.py tests/test_serve_viewer.py
git commit -m "feat(viewer): POST /api/rename-domain — renomme le dossier + patch MEMORY.md

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6 : UI — crayon de renommage de domaine dans la sidebar

**Files:**
- Modify: `assets/viewer-template.html`

- [ ] **Step 1 : Ajouter une action de renommage sur le summary d'un nœud de domaine**

Dans `navNode(node, label, depth)`, le `summary` est généré ainsi :

```javascript
  g.innerHTML = `<summary><span class="gdot" style="background:var(--coral)"></span>${esc(label)}<span class="gcount">${node.count}</span></summary>`;
```

Le remplacer par (ajoute un crayon visible au survol, seulement aux domaines de 1er niveau `depth===0`) :

```javascript
  const pencil = depth === 0 ? `<span class="grename" title="renommer le domaine">✎</span>` : '';
  g.innerHTML = `<summary><span class="gdot" style="background:var(--coral)"></span>${esc(label)}<span class="gcount">${node.count}</span>${pencil}</summary>`;
  if(depth === 0){
    const p = g.querySelector('.grename');
    p.onclick = (ev) => { ev.preventDefault(); ev.stopPropagation(); renameDomain(label); };
  }
```

- [ ] **Step 2 : Ajouter la fonction `renameDomain` + le style du crayon**

Dans le `<script>`, ajouter (près de `editFact`) :

```javascript
async function renameDomain(old){
  const nw = prompt('Renommer le domaine « ' + old +' » en :', old);
  if(!nw || nw === old) return;
  try { const meta = await api('POST', '/api/rename-domain', { old, new: nw.trim() }); applyMeta(meta); update(); }
  catch(e){ alert('Renommage refusé : ' + (e.message||e)); }
}
```

Dans le `<style>`, après la règle `.group > summary .gcount { … }`, ajouter :

```css
  .group > summary .grename { color: var(--faint); opacity: 0; margin-left: 6px; cursor: pointer; transition: opacity .12s; }
  .group > summary:hover .grename { opacity: 1; }
  .group > summary .grename:hover { color: var(--coral); }
```

(Note : `.gcount` a `margin-left:auto` qui pousse le compte à droite ; le crayon vient après, collé au compte — acceptable. Si tu préfères le crayon avant le compte, place `${pencil}` avant `<span class="gcount">`.)

- [ ] **Step 3 : Vérification manuelle**

Run :
```bash
python3 scripts/serve-viewer.py /tmp/sm-crud-test assets/viewer-template.html 8898 &
echo "Ouvrir http://127.0.0.1:8898 — survoler un domaine, cliquer ✎, renommer"
```
Vérifier : le domaine est renommé dans l'arbre, les faits suivent, pas d'erreur. Arrêter : `kill %1`.

- [ ] **Step 4 : Commit**

```bash
git add assets/viewer-template.html
git commit -m "feat(viewer): UI — renommer un domaine au survol (crayon)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 7 : Vérification d'ensemble

- [ ] **Step 1 : Suite complète**

Run : `python3 -m unittest discover -s . -p 'test_*.py' 2>&1 | tail -3`
Expected : tous les tests passent (76 précédents + CreateTest 6 + UpdateTest 4 + DeleteTest 3 + RenameDomainTest 4 = 93).

- [ ] **Step 2 : Fumée d'écriture réelle (avec jeton)**

Run :
```bash
rm -rf /tmp/sm-crud-test && mkdir -p /tmp/sm-crud-test/mailing
printf '%s' '---\nname: a\ndescription: d\nmetadata:\n  type: project\n---\nx' > /tmp/sm-crud-test/mailing/a.md
python3 scripts/serve-viewer.py /tmp/sm-crud-test assets/viewer-template.html 8898 &
sleep 1
TOKEN=$(curl -s http://127.0.0.1:8898/ | grep -oE '"token": ?"[a-f0-9]{32}"' | grep -oE '[a-f0-9]{32}')
curl -s -X POST http://127.0.0.1:8898/api/fact -H "X-SM-Token: $TOKEN" -H "Content-Type: application/json" \
  -d '{"name":"smoke","type":"project","description":"d","body":"b","domain":"mailing"}' >/dev/null
ls /tmp/sm-crud-test/mailing/   # doit contenir smoke.md
curl -s -o /dev/null -w "sans jeton: %{http_code} (attendu 403)\n" -X POST http://127.0.0.1:8898/api/fact \
  -H "Content-Type: application/json" -d '{}'
kill %1 2>/dev/null
```
Expected : `smoke.md` créé avec jeton ; `sans jeton: 403`. (Fumée manuelle ; les tests unittest couvrent déjà le contrat.)

- [ ] **Step 3 : Confirmer qu'aucune écriture ne sort du clone**

Run : `grep -n "_safe_path\|_safe_dir" scripts/serve-viewer.py | head`
Expected : toutes les écritures passent par `_safe_path`/`_safe_dir` (anti-traversal). Aucune écriture git, aucun push dans `serve-viewer.py`.

- [ ] **Step 4 : (pas de commit)** Vérification seule.

---

## Self-Review

**Couverture de la spec :**

| Élément du design | Tâche |
|---|---|
| Écriture étage 1 (clone local), aucun git/push | Tasks 1-3, 5 (écrivent dans `vault`, jamais git) |
| `POST /api/fact` créer + validations | Task 1 |
| `PUT /api/fact` éditer + **déplacer** (domaine changé) | Task 2 |
| `DELETE /api/fact` supprimer | Task 3 |
| `POST /api/rename-domain` + patch `MEMORY.md` (seul à la toucher) | Task 5 |
| Faits perso (`user`/`feedback`) forcés racine, hors index | Task 1 (`_validate`), testé |
| Jeton same-origin (`X-SM-Token`) sur les écritures | Task 1 (injecté + `_require_token`) |
| Anti-traversal réutilisé (`_safe_path`/`_safe_dir`, `.md` only) | Tasks 1, 3, 5 |
| reshard après chaque écriture (moteur d'index, préserve `MEMORY.md`) | Tasks 1-3, 5 |
| Réponse = métadonnées fraîches `{facts, index, count}` | Task 1 (`_metadata`) |
| UI : formulaire création (remplace snippet), édition/suppression, bandeau local | Task 4 |
| UI : renommage de domaine | Task 6 |
| Plan en 2 phases | Phase 1 (Tasks 1-4), Phase 2 (Tasks 5-6) |

**Cohérence des types/signatures (vérifiée) :**
- `create_fact/update_fact/delete_fact/rename_domain(vault, …)` → renvoient `_metadata(vault)` = `{facts, index, vault, count}` (+ `token` seulement dans `GET /`). Le client lit `meta.facts/index/count` (`applyMeta`).
- `_safe_path(vault, rel)` lève `ApiError(404)` ; `_validate(data)` lève `ApiError(400)` ; handlers mappent `e.status`/`e.message`.
- `ApiError(status, message)` : défini Task 1, utilisé partout.
- Format de fait `_fact_text(name, description, type_, body)` cohérent avec le frontmatter lu par `parse_md` (clé `metadata.type`).
- Routes : `POST /api/fact` (Task 1), `PUT`/`DELETE /api/fact` (Tasks 2-3), `POST /api/rename-domain` (Task 5) — chemins identiques côté serveur et côté `api()` UI.

**Placeholders :** aucun — code complet à chaque étape ; les tâches UI montrent le JS/markup exact + une procédure de vérif manuelle précise.

**Note (non bloquante) :** `reshard` renomme les fichiers de faits en `<name>.md` ; le client tient un `file` qui peut devenir périmé après une écriture → il se rafraîchit toujours via les métadonnées renvoyées (`applyMeta` + `state.factId` recalculé). Couvert dans Tasks 4.
