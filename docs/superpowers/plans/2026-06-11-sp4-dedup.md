# SP4 — Dédup sémantique à la création Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Détecter à la création (import + CRUD) qu'un fait **ressemble à un fait existant** (cosine ≥ 0.85) et le **signaler** pour mettre à jour l'existant plutôt qu'empiler un doublon.

**Architecture :** Un cœur `embed.find_similar(text, …)` (réutilise `semantic_topk`/`_pointer`), branché à deux points : `/memory-import` via `scripts/similar.py`, et le CRUD via `POST /api/similar` (serve-viewer) + un avertissement dans le formulaire de création. Signaler, jamais fusionner ; fastembed optionnel (`vector_inactive` sinon).

**Tech Stack :** Python stdlib + `embed.py` (fastembed optionnel), JS vanilla. Tests : `unittest` (`tests/test_embed.py`, `tests/test_similar.py`, `tests/test_serve_viewer.py`).

**Référence design :** `docs/superpowers/specs/2026-06-11-sp4-dedup-design.md`.

**Convention du programme :** doc + tests inclus dans « terminé ».

---

## File Structure

| Fichier | Responsabilité SP4 | Action |
|---|---|---|
| `scripts/embed.py` | + `find_similar(text, facts, store, embed_fn, threshold, k, exclude)`. | Modifier |
| `scripts/similar.py` | CLI + cœur `run(vault, text, …)` pour `/memory-import`. | Créer |
| `scripts/serve-viewer.py` | + `POST /api/similar` (sans jeton, lecture). | Modifier |
| `assets/viewer-template.html` | avertissement de quasi-doublon avant création + « créer quand même ». | Modifier |
| `skills/memory-import/SKILL.md` | vérifie les doublons avant d'écrire. | Modifier |
| `docs/domain-convention.md`, `README.md`, `docs/ARCHITECTURE.md` | documenter la dédup. | Modifier |
| `tests/test_embed.py`, `tests/test_similar.py`, `tests/test_serve_viewer.py` | tests. | Créer/Modifier |

---

# Phase 1 — Import

## Task 1 : `embed.find_similar`

**Files:**
- Modify: `scripts/embed.py`
- Test: `tests/test_embed.py`

- [ ] **Step 1 : Ajouter les tests qui échouent**

Ajouter dans `tests/test_embed.py`, avant `if __name__` (les helpers `fact` et `E` existent déjà) :

```python
class FindSimilarTest(unittest.TestCase):
    def _fe(self, texts):
        # vecteur déterministe : 'grp1' dans le texte -> direction 1, sinon direction 2
        return [[1.0, 0.0] if "grp1" in t else [0.0, 1.0] for t in texts]

    def test_above_threshold_excludes_self(self):
        facts = [fact("d/a.md", "a", "grp1 alpha", "x"),
                 fact("d/b.md", "b", "grp1 beta", "y"),
                 fact("d/c.md", "c", "grp2 gamma", "z")]
        store = E.refresh_store(facts, {}, self._fe)
        res = E.find_similar("grp1 nouveau", facts, store, self._fe, threshold=0.85, k=3, exclude="d/a.md")
        files = [r["file"] for r in res["similar"]]
        self.assertEqual(files, ["d/b.md"])        # b (cos 1.0) ; a exclu ; c (cos 0) < seuil
        self.assertNotIn("body", res["similar"][0])
        self.assertFalse(res["vector_inactive"])

    def test_no_embed_fn_inactive(self):
        res = E.find_similar("x", [], {}, None)
        self.assertTrue(res["vector_inactive"])
        self.assertEqual(res["similar"], [])
```

- [ ] **Step 2 : Lancer, vérifier l'échec**

Run : `python3 -m unittest tests.test_embed.FindSimilarTest -v`
Expected : FAIL — `module 'embed' has no attribute 'find_similar'`.

- [ ] **Step 3 : Implémenter**

Ajouter à `scripts/embed.py`, après `search` :

```python
def find_similar(text, facts, store, embed_fn, threshold=0.85, k=3, exclude=None):
    """Quasi-doublons sémantiques d'un `text` candidat : pointeurs des faits dont le cosine ≥ seuil
    (hors `exclude`), top k, triés par score. Jamais de body. embed_fn None -> vector_inactive."""
    if embed_fn is None:
        return {"similar": [], "vector_inactive": True}
    by_file = {f["file"]: f for f in facts}
    qvec = embed_fn([text])[0]
    out = []
    for file, score in semantic_topk(qvec, store, k + 5):
        if file == exclude or file not in by_file:
            continue
        if score < threshold:
            break                       # semantic_topk est trié décroissant
        out.append(_pointer(by_file[file], round(score, 4)))
        if len(out) >= k:
            break
    return {"similar": out, "vector_inactive": False}
```

- [ ] **Step 4 : Lancer, vérifier le succès**

Run : `python3 -m unittest tests.test_embed -v`
Expected : PASS (existants + `FindSimilarTest`).

- [ ] **Step 5 : Commit**

```bash
git add scripts/embed.py tests/test_embed.py
git commit -m "feat(dedup): embed.find_similar — quasi-doublons sémantiques (pointeurs, seuil)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2 : `scripts/similar.py` (CLI + cœur)

**Files:**
- Create: `scripts/similar.py`
- Test: `tests/test_similar.py`

- [ ] **Step 1 : Écrire les tests qui échouent**

Créer `tests/test_similar.py` :

```python
import importlib.util
import os
import tempfile
import unittest

HERE = os.path.dirname(__file__)
SPEC = importlib.util.spec_from_file_location("similar", os.path.join(HERE, "..", "scripts", "similar.py"))
S = importlib.util.module_from_spec(SPEC)
SPEC.loader.exec_module(S)


def fe(texts):
    return [[1.0, 0.0] if "grp1" in t else [0.0, 1.0] for t in texts]


def write_fact(vault, rel, name, desc):
    p = os.path.join(vault, rel)
    os.makedirs(os.path.dirname(p), exist_ok=True)
    with open(p, "w", encoding="utf-8") as f:
        f.write("---\nname: %s\ndescription: %s\nmetadata:\n  type: project\n---\ncorps" % (name, desc))


class SimilarRunTest(unittest.TestCase):
    def test_run_detects_near_dup(self):
        with tempfile.TemporaryDirectory() as v:
            write_fact(v, "mailing/a.md", "a", "grp1 alpha")
            res = S.run(v, "grp1 candidat", embed_fn=fe)
            self.assertIn("mailing/a.md", [r["file"] for r in res["similar"]])
            self.assertFalse(res["vector_inactive"])

    def test_run_inactive_without_embed(self):
        with tempfile.TemporaryDirectory() as v:
            write_fact(v, "mailing/a.md", "a", "desc")
            res = S.run(v, "x", embed_fn=None)
            self.assertTrue(res["vector_inactive"])
            self.assertEqual(res["similar"], [])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2 : Lancer, vérifier l'échec**

Run : `python3 -m unittest tests.test_similar -v`
Expected : FAIL — `scripts/similar.py` absent.

- [ ] **Step 3 : Créer `scripts/similar.py`**

```python
#!/usr/bin/env python3
"""Quasi-doublons sémantiques d'un texte candidat dans un vault. Réutilise embed.find_similar.

Usage : similar.py <vault> --text "<texte>" [--threshold 0.85] [--exclude <file>]
Imprime un JSON {"similar": [...], "vector_inactive": bool}.
"""
import argparse
import importlib.util
import json
import os

_HERE = os.path.dirname(os.path.abspath(__file__))


def _load(name, filename):
    spec = importlib.util.spec_from_file_location(name, os.path.join(_HERE, filename))
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod


bv = _load("build_viewer", "build-viewer.py")
embed = _load("embed", "embed.py")


def run(vault, text, threshold=0.85, exclude=None, embed_fn="auto"):
    """`embed_fn="auto"` charge fastembed ; passer un factice pour les tests, ou None."""
    facts, _ = bv.collect_facts(vault, include_body=True)
    if embed_fn == "auto":
        embed_fn = embed.load_fastembed_embed_fn()
    store = {} if embed_fn is None else embed.refresh_store(facts, {}, embed_fn)
    return embed.find_similar(text, facts, store, embed_fn, threshold=threshold, exclude=exclude)


def main():
    ap = argparse.ArgumentParser(description="Quasi-doublons d'un texte candidat dans un vault.")
    ap.add_argument("vault")
    ap.add_argument("--text", required=True)
    ap.add_argument("--threshold", type=float, default=0.85)
    ap.add_argument("--exclude", default=None)
    args = ap.parse_args()
    print(json.dumps(run(args.vault, args.text, args.threshold, args.exclude), ensure_ascii=False))


if __name__ == "__main__":
    main()
```

- [ ] **Step 4 : Lancer, vérifier le succès**

Run : `python3 -m unittest tests.test_similar -v`
Expected : PASS (2 tests).

- [ ] **Step 5 : Commit**

```bash
git add scripts/similar.py tests/test_similar.py
git commit -m "feat(dedup): scripts/similar.py — CLI quasi-doublons (cœur testable)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3 : Câbler `/memory-import`

**Files:**
- Modify: `skills/memory-import/SKILL.md`

- [ ] **Step 1 : Ajouter une étape de vérification des doublons avant l'écriture**

Dans `skills/memory-import/SKILL.md`, **avant** l'étape 5 (« Écrire un fichier par fait »), insérer une nouvelle étape (renuméroter si besoin) :

```markdown
4bis. **Vérifier les quasi-doublons** avant d'écrire chaque fait. Lancer :

   ```bash
   python3 ${CLAUDE_PLUGIN_ROOT%/}/scripts/similar.py "<clone>" --text "<nom>. <description>. <corps>"
   ```

   Si la sortie contient des `similar` (cosine ≥ 0.85), **proposer à l'utilisateur de mettre à jour
   le fait existant** (le plus proche) plutôt que d'en créer un nouveau ; ne créer un nouveau fait
   que s'il est réellement distinct. Si `vector_inactive: true` (fastembed absent), le **mentionner**
   (« dédup sémantique inactive — `pip install fastembed` ») et continuer sans bloquer.
```

- [ ] **Step 2 : Ajouter la ressource**

Dans la section `## Ressources` de `skills/memory-import/SKILL.md`, ajouter :

```markdown
- **`${CLAUDE_PLUGIN_ROOT}/scripts/similar.py`** — détecte les quasi-doublons sémantiques d'un fait
  candidat (réutilise les embeddings).
```

- [ ] **Step 3 : Vérifier**

Run : `grep -c "similar.py\|quasi-doublon\|doublon" skills/memory-import/SKILL.md`
Expected : ≥ 1.

- [ ] **Step 4 : Commit**

```bash
git add skills/memory-import/SKILL.md
git commit -m "feat(dedup): memory-import vérifie les quasi-doublons avant d'écrire

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4 : Documentation (Phase 1)

**Files:**
- Modify: `docs/domain-convention.md`
- Modify: `README.md`
- Modify: `docs/ARCHITECTURE.md`

- [ ] **Step 1 : `domain-convention.md`**

Dans `docs/domain-convention.md`, dans la section `## Ranger un fait dans un domaine`, après la puce « Garde-fou anti-prolifération » (sur les domaines), ajouter une puce :

```markdown
3bis. **Garde-fou anti-doublon (faits)** : avant d'écrire un fait, vérifier les **quasi-doublons
   sémantiques** (`scripts/similar.py`, cosine ≥ 0.85) — mettre à jour un fait proche existant
   plutôt qu'empiler un doublon. La détection se fait à la création (import + CRUD du viewer).
```

- [ ] **Step 2 : `README.md`**

À la fin de la liste de la section `## Recherche & passage à l'échelle`, ajouter :

```markdown
- **Dédup sémantique** : à la création (import / CRUD), un fait trop proche d'un existant (cosine ≥ 0.85) est **signalé** → on met à jour plutôt qu'empiler un doublon (fastembed optionnel).
```

- [ ] **Step 3 : `docs/ARCHITECTURE.md`**

À la fin de la section `## 12`, ajouter :

```markdown
### Dédup sémantique (anti-doublon)
À la création d'un fait (`/memory-import`, CRUD du viewer), `embed.find_similar` signale les
quasi-doublons (cosine ≥ 0.85, fastembed optionnel) → l'humain met à jour le fait existant plutôt
que d'empiler. On **signale, jamais on ne fusionne** (gouvernance). Scan global d'un vault déjà
peuplé : hors scope (évolution).
```

- [ ] **Step 4 : Vérifier**

Run : `grep -c "doublon\|find_similar\|Dédup\|dédup" docs/domain-convention.md README.md docs/ARCHITECTURE.md`
Expected : chaque fichier ≥ 1.

- [ ] **Step 5 : Commit**

```bash
git add docs/domain-convention.md README.md docs/ARCHITECTURE.md
git commit -m "docs(dedup): convention/README/ARCHITECTURE — dédup à la création

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5 : Vérification Phase 1

- [ ] **Step 1 : Suite complète**

Run : `python3 -m unittest discover -s . -p 'test_*.py' 2>&1 | tail -3`
Expected : tous les tests passent (111 précédents + find_similar 2 + similar 2 = 115).

- [ ] **Step 2 : Fumée — `similar.py` en réel (fastembed installé)**

Run :
```bash
d=$(mktemp -d); mkdir -p "$d/mailing"
printf '%s' '---
name: relance-panier
description: relance des paniers abandonnés après 72h
metadata:
  type: project
---
Les paniers abandonnés déclenchent une relance email après 72 heures.' > "$d/mailing/relance-panier.md"
python3 scripts/similar.py "$d" --text "courriel de relance pour les paniers non finalisés" 2>/dev/null | python3 -m json.tool
rm -rf "$d"
```
Expected : un JSON avec `relance-panier.md` dans `similar` (le candidat paraphrasé est proche). **(Pas de commit.)**

---

# Phase 2 — CRUD

## Task 6 : Serveur — `POST /api/similar`

**Files:**
- Modify: `scripts/serve-viewer.py`
- Test: `tests/test_serve_viewer.py`

- [ ] **Step 1 : Ajouter les tests qui échouent**

Ajouter dans `tests/test_serve_viewer.py`, avant `if __name__` :

```python
class SimilarEndpointTest(ServerTestBase):
    def _fake(self):
        return lambda texts: [[1.0, 0.0] if "grp1" in t else [0.0, 1.0] for t in texts]

    def test_similar_returns_near_dup(self):
        write(os.path.join(self.vault, "mailing", "x.md"),
              "---\nname: x\ndescription: grp1 alpha\nmetadata:\n  type: project\n---\ncorps")
        orig = sv.embed.load_fastembed_embed_fn
        sv.embed.load_fastembed_embed_fn = self._fake
        try:
            r = write_req(self.port, "POST", "/api/similar",
                          {"name": "y", "description": "grp1 beta", "body": "z"})
            res = json.loads(r.read().decode("utf-8"))
        finally:
            sv.embed.load_fastembed_embed_fn = orig
        self.assertFalse(res["vector_inactive"])
        self.assertIn("mailing/x.md", [s["file"] for s in res["similar"]])

    def test_similar_inactive_without_fastembed(self):
        orig = sv.embed.load_fastembed_embed_fn
        sv.embed.load_fastembed_embed_fn = lambda: None
        try:
            r = write_req(self.port, "POST", "/api/similar",
                          {"name": "y", "description": "d", "body": "z"})
            res = json.loads(r.read().decode("utf-8"))
        finally:
            sv.embed.load_fastembed_embed_fn = orig
        self.assertTrue(res["vector_inactive"])
```

(`write_req` ne passe pas de jeton par défaut → confirme que `/api/similar` n'en exige pas.)

- [ ] **Step 2 : Lancer, vérifier l'échec**

Run : `python3 -m unittest tests.test_serve_viewer.SimilarEndpointTest -v`
Expected : FAIL — pas de route `/api/similar` (404/erreur).

- [ ] **Step 3 : Implémenter dans `scripts/serve-viewer.py`**

(a) Après le chargement de `reshard` (`reshard = _load("reshard", "reshard.py")`), ajouter :

```python
embed = _load("embed", "embed.py")
```

(b) Ajouter la fonction module (après `rename_domain`) :

```python
def similar(vault, data):
    text = "\n".join(((data.get("name") or "").strip(),
                      (data.get("description") or "").strip(),
                      data.get("body", "") or ""))
    facts, _ = bv.collect_facts(vault, include_body=True)
    embed_fn = embed.load_fastembed_embed_fn()
    store = {} if embed_fn is None else embed.refresh_store(facts, {}, embed_fn)
    return embed.find_similar(text, facts, store, embed_fn, exclude=(data.get("exclude") or None))
```

(c) Dans `do_POST`, **tout au début** du corps (avant `try: self._require_token()`), traiter `/api/similar` **sans jeton** (lecture) :

```python
        def do_POST(self):
            u = urlparse(self.path)
            if u.path == "/api/similar":                 # requête en lecture : pas de jeton
                try:
                    self._ok(similar(vault, self._json_body()))
                except (ValueError, OSError) as e:
                    self._send(400, "erreur: %s" % e)
                return
            try:
                self._require_token()
                if u.path == "/api/fact":
                    self._ok(create_fact(vault, self._json_body()))
                elif u.path == "/api/rename-domain":
                    d = self._json_body()
                    self._ok(rename_domain(vault, d.get("old"), d.get("new")))
                else:
                    self._send(404, "not found")
            except ApiError as e:
                self._send(e.status, e.message)
            except (ValueError, OSError) as e:
                self._send(400, "erreur: %s" % e)
```

(Remplacer la `do_POST` existante par cette version — elle conserve `/api/fact` et `/api/rename-domain` à l'identique, en ajoutant la branche `/api/similar` en tête.)

- [ ] **Step 4 : Lancer, vérifier le succès**

Run : `python3 -m unittest tests.test_serve_viewer -v`
Expected : PASS (existants + `SimilarEndpointTest`).

- [ ] **Step 5 : Commit**

```bash
git add scripts/serve-viewer.py tests/test_serve_viewer.py
git commit -m "feat(dedup): POST /api/similar — quasi-doublons (lecture, sans jeton)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 7 : UI — avertissement de quasi-doublon avant création

**Files:**
- Modify: `assets/viewer-template.html`

UI JS, vérif **manuelle**.

- [ ] **Step 1 : Ajouter un conteneur d'avertissement au formulaire de création**

Dans la fonction `viewDraft()`, juste avant la ligne du bouton `créer le fait`
(`<div style="margin-top:10px"><button class="btn" id="d-create">…`), ajouter un conteneur :

```javascript
    <div id="d-warn"></div>
```

- [ ] **Step 2 : Vérifier les doublons avant de créer**

Remplacer le corps de `bindDraft` (la fonction entière) par :

```javascript
function bindDraft(){
  const v = id => ($(id) ? $(id).value.trim() : '');
  async function doCreate(){
    const meta = await api('POST', '/api/fact', { name:v('d-name'), description:v('d-desc'),
      type:$('d-type').value, body:$('d-body').value, domain:v('d-domain') });
    applyMeta(meta);
    state.view='fact'; state.factId = (v('d-domain') && $('d-type').value!=='user' && $('d-type').value!=='feedback')
      ? v('d-domain')+'/'+v('d-name')+'.md' : v('d-name')+'.md';
    update();
  }
  $('d-create').addEventListener('click', async () => {
    try {
      const sim = await api('POST', '/api/similar', { name:v('d-name'), description:v('d-desc'), body:$('d-body').value });
      if(sim.similar && sim.similar.length){
        $('d-warn').innerHTML = `<div class="ud-note" style="border-color:var(--reference)">
          ⚠ Ce fait ressemble à un fait existant — mets-le à jour plutôt que d'empiler un doublon :
          <div style="margin:8px 0">${sim.similar.map(s =>
            `<button class="result" data-file="${esc(s.file)}" style="margin:3px 0">
               <div class="rh"><span class="rn">${esc(s.name)}</span>
               <span class="rd" style="margin-left:auto">${(s.score*100).toFixed(0)}%</span></div></button>`).join('')}</div>
          <button class="btn" id="d-force">créer quand même</button></div>`;
        $('d-warn').querySelectorAll('.result').forEach(b =>
          b.onclick = () => { state.view='fact'; state.factId=b.dataset.file; $('q').value=''; state.query=''; update(); });
        $('d-force').onclick = async () => { try { await doCreate(); } catch(e){ alert(String(e.message||e)); } };
        return;
      }
      await doCreate();
    } catch(e){ const m=$('d-msg'); m.style.display='inline'; m.style.color='var(--feedback)'; m.textContent = String(e.message||e); }
  });
}
```

- [ ] **Step 3 : Vérification manuelle**

Run (fastembed requis pour voir l'avertissement) :
```bash
rm -rf /tmp/sm-dedup && mkdir -p /tmp/sm-dedup/mailing
printf '%s' '---
name: relance-panier
description: relance des paniers abandonnés après 72h
metadata:
  type: project
---
Les paniers abandonnés déclenchent une relance email après 72 heures.' > /tmp/sm-dedup/mailing/relance-panier.md
python3 scripts/serve-viewer.py /tmp/sm-dedup assets/viewer-template.html 8904 >/dev/null 2>&1 &
echo "Ouvrir http://127.0.0.1:8904 → '+ rédiger un fait' → nom 'relance-paniers-abandonnes', description 'courriel de relance pour paniers non finalisés' → 'créer le fait' : un AVERTISSEMENT doit lister 'relance-panier' (cliquable) + bouton 'créer quand même'."
```
Vérifier de visu, puis tuer le serveur (PID sur 8904 ; ne PAS `pkill -f serve-viewer`).

- [ ] **Step 4 : Commit**

```bash
git add assets/viewer-template.html
git commit -m "feat(dedup): UI — avertissement de quasi-doublon avant création (créer quand même)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 8 : Vérification d'ensemble

- [ ] **Step 1 : Suite complète**

Run : `python3 -m unittest discover -s . -p 'test_*.py' 2>&1 | tail -3`
Expected : tous les tests passent (115 Phase 1 + `SimilarEndpointTest` 2 = 117).

- [ ] **Step 2 : (pas de commit)** Vérification seule.

---

## Self-Review

**Couverture de la spec :**

| Élément du design | Tâche |
|---|---|
| `embed.find_similar` (≥ seuil, exclut soi, vector_inactive, pointeurs) | Task 1 |
| `scripts/similar.py` (CLI + cœur testable) | Task 2 |
| Câblage `/memory-import` | Task 3 |
| `POST /api/similar` (sans jeton, lecture) | Task 6 |
| UI : avertissement + « créer quand même » | Task 7 |
| Signaler jamais fusionner ; seuil 0.85 ; fastembed optionnel | Tasks 1, 3, 6, 7 |
| Doc (convention/README/ARCHITECTURE) | Task 4 |
| Tests (find_similar, similar, /api/similar) | Tasks 1, 2, 6 |
| Plan en 2 phases (import / CRUD) | Phase 1 (Tasks 1-5), Phase 2 (Tasks 6-8) |

**Cohérence des types/signatures :**
- `find_similar(text, facts, store, embed_fn, threshold=0.85, k=3, exclude=None) -> {similar, vector_inactive}` : Task 1, appelée par `similar.run` (Task 2) et `serve-viewer.similar` (Task 6).
- Pointeur `{file, name, path, score}` (via `_pointer`) : jamais de body (Task 1, testé).
- `run(vault, text, threshold, exclude, embed_fn="auto") -> {similar, vector_inactive}` : Task 2, monkeypatchable.
- `embed_fn` factice `[1,0] si grp1 sinon [0,1]` : même convention dans tests Task 1/2/6.
- serve-viewer charge `embed` via `_load` (Task 6) — `sv.embed.load_fastembed_embed_fn` monkeypatchable (comme les tests MCP).

**Placeholders :** aucun — code complet à chaque étape.

**Note (non bloquante) :** `similar.run` / `serve-viewer.similar` ré-embeddent tous les faits à chaque
appel (store en mémoire, non persistant) — acceptable à l'échelle d'un vault local et d'un appel
interactif par création ; une persistance par slug est une optimisation future.
